// Core pipeline entry point (see docs/PROJECT_PLAN.md and
// docs/ARCHITECTURE.md, section 2).
//
// Will wire the stages below into one function: seed + params -> city
// model. No DOM, no Node-only APIs — must run unchanged in Node and in a
// browser.
//
//   terrain -> roads -> parcels -> buildings -> project
//
// Not implemented yet.

export {};
