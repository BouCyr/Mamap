// Projection stage (see docs/PROJECT_PLAN.md, phase 7).
//
// Flattens the full 3D city model (terrain + street graph + parcels +
// buildings) into a 2D map description: lines, polygons, and fills in 2D
// map coordinates. This is the boundary between core generation and
// rendering — nothing 3D should be left in its output.
//
// Not implemented yet. See docs/ARCHITECTURE.md, section 2, for the exact
// data contract this stage is expected to produce.

export {};
