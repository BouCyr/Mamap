// Roads stage (see docs/PROJECT_PLAN.md, phase 3).
//
// Takes the terrain heightmap and returns a street graph: nodes (points,
// with elevation looked up from the heightmap) and edges (road segments,
// each tagged with a width/class such as main road vs. minor street).
//
// Not implemented yet. See docs/ARCHITECTURE.md, section 2, for the exact
// data contract this stage is expected to produce.

export {};
