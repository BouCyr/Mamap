// Roads stage (see docs/PROJECT_PLAN.md, phase 4).
//
// Takes the point mesh and its per-point elevation, returns a street graph:
// nodes (mesh points, with elevation) and edges (road segments, each tagged
// with a width/class such as main road vs. minor street).
//
// Not implemented yet. See docs/ARCHITECTURE.md, section 2, for the exact
// data contract this stage is expected to produce.

export {};
