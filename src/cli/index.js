// Node command-line entry point (see docs/PROJECT_PLAN.md, phase 11, and
// docs/ARCHITECTURE.md, section 5).
//
// Will take parameters (seed, size, ...), run the core pipeline, run
// render/two-d in its Node mode, and write the result to a file. No
// generation logic of its own — only wiring.
//
// Not implemented yet.

export {};
