// 2D map rendering (see docs/PROJECT_PLAN.md, phase 8, and
// docs/ARCHITECTURE.md, section 4).
//
// Takes the flattened 2D map description (core/project's output) and turns
// it into the final map. Current plan: build SVG, so the same builder code
// either serializes to a string/file (Node) or attaches live to a page
// (browser). Must work in both environments.
//
// Not implemented yet.

export {};
