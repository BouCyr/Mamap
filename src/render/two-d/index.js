// 2D map rendering (see docs/PROJECT_PLAN.md, phase 10, and
// docs/ARCHITECTURE.md, section 4).
//
// Takes the flattened 2D map description (core/project's output) and turns
// it into the final 5000 x 5000 SVG map, so the same builder code either
// serializes to a string/file (Node) or attaches live to a page (browser).
// Must work in both environments.
//
// Not implemented yet.

export {};
