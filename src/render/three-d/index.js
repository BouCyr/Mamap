// Optional 3D preview (see docs/PROJECT_PLAN.md, phase 6, and
// docs/ARCHITECTURE.md, section 4).
//
// Browser-only: draws the full city model (terrain + roads + building
// volumes) using raw WebGL, so the generator's output can be checked
// visually before it is flattened to 2D. Not the final deliverable, and
// never imported from Node.
//
// Not implemented yet.

export {};
