// Optional 3D preview (see docs/PROJECT_PLAN.md, phase 12, and
// docs/ARCHITECTURE.md, section 4).
//
// Browser-only: draws the full city model (mesh + elevation + roads +
// building volumes) using raw WebGL, so the generator's output can be
// checked visually before it is flattened to 2D. Not the final
// deliverable, and never imported from Node.
//
// Not implemented yet.

export {};
